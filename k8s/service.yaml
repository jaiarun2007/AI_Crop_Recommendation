apiVersion: v1
kind: Service
metadata:
  name: farming-assistant
  namespace: ai-crop-recommendation
spec:
  selector:
    app: farming-assistant
  ports:
    - port: 80
      targetPort: 8000
  type: ClusterIP
